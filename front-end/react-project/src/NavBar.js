import React from 'react';
import { NavLink } from 'react-router-dom';

const NavBar = () => {

    // map over projects for user from fetch request to make tabs to display projects
}

export default NavBar;